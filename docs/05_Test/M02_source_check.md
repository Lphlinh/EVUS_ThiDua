# M02 source check v7

- Python syntax check: PASS.
- Updated teacher scoring screen:
  - Header top spacing adjusted.
  - Criteria records include normalized header aliases.
  - Scoring form rebuilt with Streamlit columns so criterion content, default score, teacher score, and note display in the same row.
  - Sticky form header added.
  - Existing zero-score detail rows are prefilled from corrected default score.
  - Saving updates `DiemMacDinh`, `DiemGV`, and `GhiChuGV`.
